name: 'dev_marketing_measurement'
version: '1.0.0'
config-version: 2

profile: 'dev_marketing_measurement'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dev_marketing_measurement:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
